Dockerfile
.dockerignore

# Host binary from `go build -o server ./cmd/server`. Compiled for darwin/arm64
# and useless in a linux container, which is why the image builds its own.
server

coverage.out

.DS_Store
.idea/
.vscode/